# 关于页字体放大

针对截图两处（仅改 About.vue 样式）：
1. **公司介绍正文**（`.intro p`）：由默认 1rem 提升到 1.25rem（font-lg），行高 1.9
2. **发展历程时间线**（`.timeline p`）：提升到 1.15rem；年份时间戳（el-timeline timestamp）通过深度选择器放大到 1.05rem 加粗

改完 build 验证 → 提交推送。