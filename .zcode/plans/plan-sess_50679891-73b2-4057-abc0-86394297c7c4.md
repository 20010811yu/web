实施上述 P0–P2 优化（P3/第9项可按需跟进）：

1. 修复部署地址不一致：统一 sitemap.xml、admin/config.yml site_url、robots.txt 的域名（以 Netlify 实际域名为准）。
2. MobileContactBar.vue 硬编码「微信」接入 i18n（zh/en 同步补 key）。
3. 新增 ESLint（flat config + vue 插件）与 Prettier，添加 lint/format 脚本，修复暴露的问题。
4. 新增 GitHub Actions CI：push/PR 自动 npm ci + lint + build。
5. 全站 <img> 补 width/height 属性防 CLS。
6. 删除未引用的 old-banner.webp；清理 main.css 中未使用的 Inter 字体声明。
7. 新增独立 NotFound 404 页面并接入路由。
8. seo.js 增加动态 OG/canonical 更新；index.html 补 og:url、twitter:card。
9. 按 AGENTS.md P4/P5 流程：npm run build 验证 → 更新记忆库（activeContext/progress/errorlog 视情况）→ git add/commit/push。